#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -z "$NODE_PATH" ]; then
  export NODE_PATH="/Users/wangquan/Downloads/micro-enterprise-demo/node_modules/.pnpm/node_modules"
else
  export NODE_PATH="$NODE_PATH:/Users/wangquan/Downloads/micro-enterprise-demo/node_modules/.pnpm/node_modules"
fi
if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../node_modules/.pnpm/react-app-rewired@2.2.1_react-scripts@5.0.1/node_modules/react-app-rewired/bin/index.js" "$@"
else
  exec node  "$basedir/../../../../node_modules/.pnpm/react-app-rewired@2.2.1_react-scripts@5.0.1/node_modules/react-app-rewired/bin/index.js" "$@"
fi
