import React from "react";

const SubAppContainer: React.FC = () => {
  return (
    <div
      id="subapp-viewport"
      style={{
        minHeight: 360,
        background: "#fff",
        borderRadius: "8px",
        padding: "16px",
      }}
    />
  );
};

export default SubAppContainer;
